export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col items-center justify-center gap-6 px-6 text-center">
      <h1 className="text-4xl font-bold text-brand">gather.photo</h1>
      <p className="text-lg text-neutral-600">
        Every photo and video your guests capture, in one private gallery. One
        QR code. No app, no login.
      </p>
      <p className="text-sm text-neutral-400">
        Landing page — see PRD.md / TECH_SPEC.md. Host onboarding lands in M0.
      </p>
    </main>
  );
}
